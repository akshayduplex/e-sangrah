// const socket = io();

// // Global notification handler
// socket.on("notification", (data) => {
//     // fallback values
//     const { message = "Notification", type = "info" } = data;
//     showToast(message, type);
// });

// // Global helper (client-side trigger if needed)
// function notify(message, type = "info") {
//     showToast(message, type);
// }