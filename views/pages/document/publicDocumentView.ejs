<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= document?.metadata?.fileName || "Files" %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        body {
            background-color: #ffffff;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            padding: 20px;
        }

        .main-header {
            font-size: 2rem;
            font-weight: 300;
            color: #333;
            margin-bottom: 5px;
        }

        .breadcrumb {
            --bs-breadcrumb-divider: '/';
            color: #6c757d;
            font-size: 0.9rem;
        }

        .breadcrumb-item a {
            color: #6c757d;
            text-decoration: none;
        }

        .breadcrumb-item.active {
            color: #333;
            font-weight: 500;
        }

        .file-card {
            width: 150px;
            padding: 15px 10px;
            background-color: #ffffff;
            border: 1px solid #dee2e6;
            border-radius: 6px;
            position: relative;
            margin-right: 15px;
        }

        .file-icon-box {
            width: 50px;
            height: 60px;
            background-color: #e0f0ff;
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 5px;
            overflow: hidden;
        }

        .file-icon-box img {
            width: 40px;
            height: 40px;
            object-fit: contain;
        }

        .file-name {
            font-size: 0.9rem;
            font-weight: 600;
            color: #333;
            margin: 0;
            line-height: 1.2;
            overflow: hidden;
            white-space: nowrap;
            text-overflow: ellipsis;
            max-width: 100%;
        }

        .file-size {
            font-size: 0.75rem;
            color: #6c757d;
            margin: 0;
        }

        .dropdown-toggle::after {
            display: none;
        }
    </style>
</head>

<body>

    <div class="container-fluid">
        <div class="text-start mb-3">
            <img src="<%= logo %>" alt="Company Logo" style="height: 60px;">
        </div>
        <h2 class="main-header" id="document-name">
            <%= document?.metadata?.fileName || 'Document' %>
        </h2>
        <div class="breadcrumb-item active" id="current-version">
            Version <%= document?.currentVersionLabel || 'N/A' %>
        </div>
        <hr>

        <% if (docExpired) { %>
            <div class="alert alert-warning mt-3" role="alert">
                <%= expiredMessage %>
            </div>
            <% } else { %>
                <div id="files-list" class="d-flex flex-wrap">
                    <% if (versionData?.files?.length> 0) { %>
                        <% versionData.files.forEach(file=> { %>
                            <div class="file-card shadow-sm">
                                <div class="d-flex flex-column align-items-center">
                                    <div class="file-icon-box">
                                        <% let ext=file.originalName.split('.').pop().toLowerCase(); let
                                            iconPath='/img/icons/fn1.png' ; if (['ppt','pptx'].includes(ext))
                                            iconPath='/img/icons/fn1.png' ; else if (['doc','docx'].includes(ext))
                                            iconPath='/img/icons/fn2.png' ; else if (['xls','xlsx'].includes(ext))
                                            iconPath='/img/icons/fn3.png' ; else if (ext==='pdf' )
                                            iconPath='/img/icons/fn4.png' ; %>
                                            <img src="<%= iconPath %>" alt="<%= ext %> icon">
                                    </div>
                                    <div class="w-100">
                                        <p class="file-name text-center mt-1" title="<%= file.originalName %>">
                                            <%= file.originalName %>
                                        </p>
                                        <p class="file-size text-center">
                                            <%= file.fileSize ? (file.fileSize / 1024).toFixed(1) + ' KB' : '0 KB' %>
                                        </p>
                                    </div>
                                </div>
                                <div class="dropdown position-absolute top-0 end-0 m-1">
                                    <button class="btn btn-sm p-0 text-dark" type="button" data-bs-toggle="dropdown"
                                        aria-expanded="false" title="Options">
                                        <i class="bi bi-three-dots"></i>
                                    </button>
                                    <ul class="dropdown-menu dropdown-menu-end">
                                        <li><a class="dropdown-item" href="/api/download/file/<%= file._id %>"><i
                                                    class="bi bi-download me-2"></i>Download</a></li>
                                        <li><a class="dropdown-item" href="/folders/view/<%= file._id %>"
                                                target="_blank"><i class="bi bi-eye me-2"></i>View</a></li>
                                    </ul>
                                </div>
                            </div>
                            <% }) %>
                                <% } else { %>
                                    <p class="text-muted mt-3">No files attached to this version.</p>
                                    <% } %>
                </div>
                <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>