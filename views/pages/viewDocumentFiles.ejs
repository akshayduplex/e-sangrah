<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>File Preview - <%= file ? file.originalName : 'No File' %>
    </title>
    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            height: 100%;
            background-color: #000;
        }

        iframe,
        img {
            display: block;
            width: 100%;
            height: 100%;
            border: none;
        }

        img {
            object-fit: contain;
            background-color: #000;
        }

        /* Optional: center "file not found" text */
        .fallback {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100%;
            color: #fff;
            font-family: Arial, sans-serif;
            text-align: center;
        }
    </style>
</head>

<body>
    <% if (!file || !file.fileUrl) { %>
        <div class="fallback">
            <div>
                <h2>File not available</h2>
                <% if (expiredMessage) { %>
                    <p>
                        <%= expiredMessage %>
                    </p>
                    <% } else { %>
                        <p>The file could not be loaded.</p>
                        <% } %>
            </div>
        </div>
        <% } else { %>

            <% if (file.fileType.isPDF) { %>
                <!-- PDF Preview -->
                <iframe src="/api/file/pdf/<%= file._id %>" title="PDF Preview of <%= file.originalName %>"></iframe>

                <% } else if (file.fileType.isWord || file.fileType.isExcel || file.fileType.isPowerPoint) { %>
                    <!-- Office Preview -->
                    <iframe
                        src="https://view.officeapps.live.com/op/embed.aspx?src=<%= encodeURIComponent(file.fileUrl) %>"
                        title="Office Preview of <%= file.originalName %>"></iframe>

                    <% } else if (file.originalName.match(/\.(jpg|jpeg|png|gif|webp)$/i)) { %>
                        <!-- Image Preview -->
                        <img src="<%= file.fileUrl %>" alt="<%= file.originalName %>">

                        <% } else { %>
                            <!-- Other file types -->
                            <div class="fallback">
                                <div>
                                    <h2>Preview not available</h2>
                                    <p>This file type (.<%= file.fileType.extension %>) cannot be previewed in the
                                            browser.</p>
                                    <p><a href="<%= file.fileUrl %>" style="color:#0af;" target="_blank">Download
                                            File</a></p>
                                </div>
                            </div>
                            <% } %>

                                <% } %>
</body>

</html>